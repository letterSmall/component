{%if section.settings.show_cart_bar%}
<div class="pccc-container sale-pccc-contianer">
<div class=" pc-product-cart-container">
<div class="page-width">
 <div class="pccci-box">
  <div class="pccc-left">
    {{section.settings.title}}
  </div>
   <div class="pccc-right">
      {%assign product=all_products[section.settings.product]%}
     {%- assign current_variant = product.selected_or_first_available_variant -%}
     {%assign section_id= product.id%}
    
     {%if product!=blank%}
{%- assign customDesc = product.metafields.custom_desc -%}
{% capture form_id %}AddToCartForm-{{ section_id }}{% endcapture %}
{% form 'product', product, id: form_id, class: 'product-single__form pccc-product-single__form' %}
  {% comment %}
    Automatically inserted by Shop Pay Installments theme updater
  {% endcomment %}
  {% unless settings.disable_autogenerated_payment_terms %}
    {{ form | payment_terms }}
  {% endunless %}
  <div class="hide">
     {% unless product.has_only_default_variant %}
    {%- for option in product.options_with_values -%}

      {%- if settings.product_color_swatches -%}
        {%- assign is_color = false -%}
        {%- assign color_option_index = 0 -%}
        {%- assign swatch_trigger = 'products.general.color_swatch_trigger' | t | downcase-%}
        {%- assign color_option_index = forloop.index0 -%}
        {%- assign downcased_option = option.name | downcase -%}
        {%- if downcased_option contains swatch_trigger -%}
          {%- assign is_color = true -%}
        {%- elsif swatch_trigger == 'color' and downcased_option contains 'colour' -%}
          {%- assign is_color = true -%}
        {%- endif -%}
      {%- endif -%}

      {% if settings.variant_type == 'button' %}
        {% render 'variant-button',
          section_id: section_id,
          option: option,
          forloop: forloop,
          is_color: is_color,
          color_option_index: color_option_index
        %}
      {% else %}
        {% render 'variant-dropdown',
          section_id: section_id,
          option: option,
          forloop: forloop
        %}
      {% endif %}

    {%- endfor -%}
  {% endunless %}

  <select name="id" id="ProductSelect-{{ section_id }}" class="product-single__variants no-js">
    {% for variant in product.variants %}
      {% if variant.available %}
        <option {% if variant == product.selected_or_first_available_variant %}
          selected="selected" {% endif %}
          value="{{ variant.id }}">
          {{ variant.title }} - {{ variant.price | money_with_currency }}
        </option>
      {% else %}
        <option disabled="disabled">
          {{ variant.title }} - {{ 'products.product.sold_out' | t }}
        </option>
      {% endif %}
    {% endfor %}
  </select>

  {% render 'quantity-input', id: section_id, qty: 1, min: 1 %}
     </div>
  {%- assign enable_dynamic_buttons = false -%}
  {% if settings.enable_payment_button and template != 'product.preorder' %}
    {%- assign enable_dynamic_buttons = true -%}
  {% endif %}

 

    {%- assign button_text = 'products.product.add_to_cart' | t -%}
    {%- if current_variant.available -%}
      {%- if template == 'product.preorder' -%}
        {%- assign button_text = 'products.product.preorder' | t -%}
      {%- endif -%}
    {%- else -%}
      {%- assign button_text = 'products.product.sold_out' | t -%}
    {%- endif -%}

  

      <button
      {% if product.empty? %}type="button"{% else %}type="submit"{% endif %}
      name="add"
      id="AddToCart-{{ section_id }}"
      class=" {% if enable_dynamic_buttons %}btn--tertiary{% else %}btn{% endif %} btn--full btn--no-animate  product-add-to-cart add-to-cart"
      {% unless current_variant.available %} disabled="disabled"{% endunless %}>
      <span id="AddToCartText-{{ section_id }}" data-default-text="{{ button_text }}">
        {%render 'icon-small-cart'%}  {{ button_text }}
      </span>
    </button>
     
     

  <textarea id="VariantsJson-{{ section_id }}" class="hide" aria-hidden="true">
    {{ product.variants | json }}
  </textarea>


  {% if product.options.size > 1 %}
    <textarea id="CurrentVariantJson-{{ section_id }}" class="hide" aria-hidden="true">
      {{ current_variant | json }}
    </textarea>
  {% endif %}
{% endform %}
{%endif%}
     <div class="pcccc-shop-btn">
       <a href="{{product.url|default:section.settings.porduct_link}}" class="btn blue-btn"> SHOP NOW</a>
     </div>
      </div>
  </div>
</div>
 
</div>
   <div class="pc-product-cart-container-shadow"></div>
</div>
{%endif%}
<div class="pccc-content-box">

{{ page.content }}
</div>



   {%assign mpspb_num=0%}
    {%for block in section.blocks%}
   {%assign mpspb_num=mpspb_num|plus:1%}
    {%endfor%}

{%if section.settings.show_hot_product%}
 {%unless mpspb_num<1 %}
<div class="more-ps-product-box" >
  <div class="page-width">  
  <div class="swiper-container mpspb-swiper-container" data-num="{{mpspb_num}}">
  <div class="swiper-wrapper  ">
       {%- assign grid_item_width = 'small--one-whole medium-up--one-whole' -%}
    {%for block in section.blocks%}
      {%assign product=all_products[block.settings.product]%}
    <div class="swiper-slide">
      {% render 'product-grid-item'
              product: product,
              grid_item_width: grid_item_width
            %}
    </div>
   {%endfor%}
  </div>
    <div class="swiper-button-prev mpspb-prev">{%render "svg-arrow-left"%}</div>
    <div class="swiper-button-next mpspb-next">{%render "svg-arrow-right"%}</div>
</div>
   
</div>
  </div>
 {%endunless%}
{%endif%}


<div class="pccc-hot-product-contianer">
  <a href="{{section.settings.hot_porduct_link}}" class="btn blue-btn">
    {{section.settings.hot_porduct_label}}
  </a>
</div>



<style>
  img{
   vertical-align: bottom;
  }
  </style>
{% schema %}
  {
    "name": "Petlibro granary",
    "settings": [
{
        "type": "checkbox",
         "id": "show_cart_bar",
        "label": "Show cart bar",
"default":false
      },
{
        "type": "checkbox",
         "id": "show_hot_product",
        "label": "Show Product",
"default":false
      },
{
        "type": "html",
         "id": "title",
        "label": "title"
      },
     {
        "type": "product",
         "id": "product",
        "label": "Product"
      },
{
        "type": "url",
         "id": "porduct_link",
        "label": "Product link"
      },
 {
        "type": "text",
         "id": "hot_porduct_label",
        "label": "Hot product label",
"default":"Find More Hot Products"
      },
{
        "type": "url",
         "id": "hot_porduct_link",
        "label": "Hot product link"
      }
],
   "blocks": [{
          "type": "product",
          "name": "Product",
          "settings": [{
            "type": "product",
            "id": "product",
            "label": "Product"
        }]
      }]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
